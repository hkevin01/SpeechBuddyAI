<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
             xmlns:views="clr-namespace:SpeechBuddyAI.Views"
             x:Class="SpeechBuddyAI.Pages.HomePage"
             ios:Page.UseSafeArea="True"
             Title="Home">
    <ScrollView>
        <VerticalStackLayout Style="{StaticResource PageRootLayoutStyle}">
            <Label Text="SpeechBuddy AI" Style="{StaticResource PageTitleLabelStyle}" />
            <Label Text="Therapist companion for articulation practice, progress tracking, and smart notes." Style="{StaticResource SupportingTextLabelStyle}" />

            <Frame Style="{StaticResource CardFrameStyle}">
                <VerticalStackLayout Spacing="6">
                    <Label Text="Session Overview" Style="{StaticResource SectionTitleLabelStyle}" />
                    <Grid x:Name="OverviewMetricsGrid" ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" ColumnSpacing="8" RowSpacing="8">
                        <Frame x:Name="TotalAttemptsCard" Grid.Row="0" Grid.Column="0" Style="{StaticResource MetricCardFrameStyle}" BackgroundColor="{StaticResource InfoContainer}" BorderColor="{StaticResource InfoOutline}">
                            <VerticalStackLayout Spacing="2">
                                <Label Text="Total Attempts" Style="{StaticResource CaptionLabelStyle}" TextColor="#555" />
                                <Label x:Name="TotalAttemptsLabel" Text="-" Style="{StaticResource MetricValueLabelStyle}" />
                            </VerticalStackLayout>
                        </Frame>
                        <Frame x:Name="AverageScoreCard" Grid.Row="0" Grid.Column="1" Style="{StaticResource MetricCardFrameStyle}" BackgroundColor="{StaticResource SuccessContainer}" BorderColor="{StaticResource SuccessOutline}">
                            <VerticalStackLayout Spacing="2">
                                <Label Text="Average Score" Style="{StaticResource CaptionLabelStyle}" TextColor="#555" />
                                <Label x:Name="AverageScoreLabel" Text="-" Style="{StaticResource MetricValueLabelStyle}" />
                            </VerticalStackLayout>
                        </Frame>
                        <Frame x:Name="MostPracticedCard" Grid.Row="1" Grid.Column="0" Style="{StaticResource MetricCardFrameStyle}" BackgroundColor="{StaticResource WarningContainer}" BorderColor="{StaticResource WarningOutline}">
                            <VerticalStackLayout Spacing="2">
                                <Label Text="Most Practiced" Style="{StaticResource CaptionLabelStyle}" TextColor="#555" />
                                <Label x:Name="MostPracticedLabel" Text="-" FontAttributes="Bold" />
                            </VerticalStackLayout>
                        </Frame>
                        <Frame x:Name="StreakCard" Grid.Row="1" Grid.Column="1" Style="{StaticResource MetricCardFrameStyle}" BackgroundColor="{StaticResource ReviewContainer}" BorderColor="{StaticResource ReviewOutline}">
                            <VerticalStackLayout Spacing="2">
                                <Label Text="Practice Streak" Style="{StaticResource CaptionLabelStyle}" TextColor="#555" />
                                <Label x:Name="StreakLabel" Text="-" FontAttributes="Bold" />
                            </VerticalStackLayout>
                        </Frame>
                    </Grid>
                    <Label x:Name="MostImprovedLabel" Text="Most improved: -" Style="{StaticResource SupportingTextLabelStyle}" />
                </VerticalStackLayout>
            </Frame>

                <Frame Style="{StaticResource CardFrameStyle}">
                <VerticalStackLayout Spacing="8">
                    <Label Text="Home Assignment" Style="{StaticResource SectionTitleLabelStyle}" />
                    <views:StatusBannerView x:Name="AssignmentStatusBanner" />
                    <Button Text="Generate from Weak Patterns"
                            TabIndex="0"
                        SemanticProperties.Description="Generate a home assignment using recent weak-pattern evidence"
                            Clicked="OnGenerateAssignmentClicked" />
                    <Label x:Name="AssignmentTitleLabel" Text="No assignment generated yet." FontAttributes="Bold" />
                    <Label x:Name="AssignmentRationaleLabel" Text="" />
                    <Label x:Name="AssignmentTargetsLabel" Text="" />
                    <Label x:Name="AssignmentWordsLabel" Text="" />
                    <Label x:Name="AssignmentReasonDetailsLabel" Text="" Style="{StaticResource CaptionLabelStyle}" />
                </VerticalStackLayout>
            </Frame>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
